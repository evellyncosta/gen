version: '3.8'

services:
  java:
    build: 
      context: .
      dockerfile: Dockerfile      
    image: tomcat
    container_name: gen
    ports:
      - "8080:8080"
    volumes:
      - './target:/usr/local/tomcat/webapps'
networks:
  dev-network:
    driver: bridge
